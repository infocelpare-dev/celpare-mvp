# dependencies
/node_modules
/.pnp
.pnp.*
.yarn/*

# next.js
/.next/
/out/
next-env.d.ts

# production
/build

# testing
/coverage
/test-results
/playwright-report
/blob-report
/playwright/.cache

# env files, never commit. Including .env.example: the repo is public, and the
# template still named the Supabase project and described how the Turnstile
# keys are split. The variable set is documented locally instead.
.env*

# vercel
.vercel

# typescript
*.tsbuildinfo

# misc
.DS_Store
*.pem
npm-debug.log*
yarn-debug.log*
yarn-error.log*
.idea/
.vscode/*
!.vscode/extensions.json

# sentry
.sentryclirc

# Internal working documents. Kept local, never published.
# The repo is public, so the product strategy, pricing, decisions and open
# questions stay off it. Everything here still exists on disk.
#
# TRACKER.md and docs/ came out of this list on 2026-09-17 and went back in on
# 2026-09-18, on founder instruction. They stay ignored. docs/context/09-gaps.md
# in particular is a list of where this system is weak, and a public repository
# is the wrong place for it. Keep them local and keep writing them: /resume and
# /handoff read them off disk and never need git.
.claude/
CLAUDE.md
AGENTS.md
TRACKER.md
docs/
